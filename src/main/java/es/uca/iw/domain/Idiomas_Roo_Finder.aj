// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package es.uca.iw.domain;

import es.uca.iw.domain.Idiomas;
import es.uca.iw.domain.Usuario;
import javax.persistence.EntityManager;
import javax.persistence.TypedQuery;

privileged aspect Idiomas_Roo_Finder {
    
    public static Long Idiomas.countFindIdiomasesByUsuario(Usuario usuario) {
        if (usuario == null) throw new IllegalArgumentException("The usuario argument is required");
        EntityManager em = Idiomas.entityManager();
        TypedQuery q = em.createQuery("SELECT COUNT(o) FROM Idiomas AS o WHERE o.usuario = :usuario", Long.class);
        q.setParameter("usuario", usuario);
        return ((Long) q.getSingleResult());
    }
    
    public static TypedQuery<Idiomas> Idiomas.findIdiomasesByUsuario(Usuario usuario) {
        if (usuario == null) throw new IllegalArgumentException("The usuario argument is required");
        EntityManager em = Idiomas.entityManager();
        TypedQuery<Idiomas> q = em.createQuery("SELECT o FROM Idiomas AS o WHERE o.usuario = :usuario", Idiomas.class);
        q.setParameter("usuario", usuario);
        return q;
    }
    
    public static TypedQuery<Idiomas> Idiomas.findIdiomasesByUsuario(Usuario usuario, String sortFieldName, String sortOrder) {
        if (usuario == null) throw new IllegalArgumentException("The usuario argument is required");
        EntityManager em = Idiomas.entityManager();
        StringBuilder queryBuilder = new StringBuilder("SELECT o FROM Idiomas AS o WHERE o.usuario = :usuario");
        if (fieldNames4OrderClauseFilter.contains(sortFieldName)) {
            queryBuilder.append(" ORDER BY ").append(sortFieldName);
            if ("ASC".equalsIgnoreCase(sortOrder) || "DESC".equalsIgnoreCase(sortOrder)) {
                queryBuilder.append(" ").append(sortOrder);
            }
        }
        TypedQuery<Idiomas> q = em.createQuery(queryBuilder.toString(), Idiomas.class);
        q.setParameter("usuario", usuario);
        return q;
    }
    
}
